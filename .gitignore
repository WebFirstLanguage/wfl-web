# SQLite database (created on first run) + WAL/SHM sidecars
scriptorium.db
scriptorium.db-*

# Uploaded media (runtime) — keep the directory, ignore its contents
static/uploads/*
!static/uploads/.gitkeep

# WFL debug reports (created on runtime errors)
*_debug.txt

# OS / editor cruft
.DS_Store
Thumbs.db
